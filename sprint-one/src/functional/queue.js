var Queue = function() {
  var someInstance = {
  };

  // Use an object with numeric keys to store values
  var storage = {
    one: someInstance.enqueue = function(value) {

    },
    two: someInstance.dequeue = function() {
    },

    three: someInstance.size = function() {
    }
  };

  // Implement the methods below

  // someInstance.enqueue = function(value) {

  // };

  // someInstance.dequeue = function() {
  // };

  // someInstance.size = function() {
  // };

  return someInstance;
};
